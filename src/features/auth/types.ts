export type UserInfo = {
  username: string;
  image: string;
  email: string;
  userId: string;
};

export type UserPasswordData = {
  oldPassword: string;
  newPassword: string;
};
export type User = {
  email: string;
  username: string;
  image: string;
  userId: string;
};
